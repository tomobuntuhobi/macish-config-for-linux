
- install git
  - sudo apt update && sudo apt install git 
  - git config --global user.name tomobuntu && git config --global user.email tomobuntu@abc.com
  - git config --global credential.helper store
    - [caution] this uses plain textfile to store credentials so be aware of risks
