# macish-config-for-linux
macish config for linux


- keyboard related settings in this repo requires user to map their keyboard to use ctrl as mac's cmd 
    - (map the key thats positioned similarly to mac's cmd to control key)